/**
in javascript primitive values like "string" are treated like objects for the soul pourpos of being able 
to use methods and properties on them. this allows us to manipulate strings in mainly 3 differnt ways

string.slice(start, end)
string.substring(start, end)
string.substr(start, length)

 */